package com.arcana.game.core.ecs;

/**
 * Base interface for all ECS components.
 * Components are pure data containers in the ECS architecture.
 */
public interface Component {
    // Marker interface - no methods required
} 